# 자동화 업데이트 V2 변경 내역

## V2 추가

- 저장 공간 부족 환경을 위한 **핵심 파일 선택 추출** 추가
- game1과 game2를 한 번에 풀지 않고 순차 분석
- 분석 완료 후 ISO 추출본 자동 삭제
- 2.5GB 이상을 차지하던 재생성 가능 중간 자료의 허용 목록 기반 안전 정리
- 원본 ISO, 번역 마스터, 한글 배정표, V4/V5 산출물 보호 검사
- 백그라운드 실행·상태 확인 스크립트 추가
- 실행 로그를 날짜별로 자동 저장
- V4 기준 파일이 없을 때 V5를 거짓 실패/성공으로 표시하지 않고 명확히 건너뜀
- 디스크 여유 공간을 doctor 보고서에 추가
- 자동 테스트를 11개로 확대

## 기존 완료 항목

- PSP 공통 ISO/CSO/추출 폴더 분석 엔진
- PARAM.SFO 파서와 게임 이미지 비교 보고서
- 프리니 SYSTEM.DAT → start.lzs → start.dat 구조 자동 검사
- 프리니 런타임 폰트 구조 자동 검사
- 프리니 1/2 호환성 A/B/C 자동 판정
- BOOT.BIN/EBOOT.BIN Shift-JIS UI 문자열 후보 스캐너
- 번역 마스터 바이트/글리프/제어코드/일본어/폭 QA
- 기존 마스터 밖 일본어 후보와 화면 검수 CSV
- V5 빌드 스크립트 경로 하드코딩 제거
- V5 입력 SHA1, START/SYSTEM/ISO 무결성 검사 강화
- V5에서 확정된 짧은 문자열 2개 자동 수정
- PPSSPP 자동 실행을 선택 사항으로 변경
- 실패 시 JSON 오류 보고서 생성
- `core/nsf_string.py` 문법 복구

## 현재 검증 결과

- 번역 CSV 4,110행
- 자동 수정 후 오류 0, 경고 0
- START 미포함 일본어 후보 80개
- 기존 BOOT 문자열 자료에서 일본어 UI 후보 566개
- 안전 정리 미리보기에서 약 2.5GiB 재생성 자료 식별
- Python 문법 검사 통과
- pytest 11개 통과

## 로컬에서만 가능한 최종 단계

`game.iso`와 `game2.iso`는 업로드 ZIP에 포함되지 않았으므로 실제 프리니 2 등급과 최종 ISO 실행 검증은 사용자 PC에서 자동 실행된다. 중간 명령 입력은 필요하지 않으며, 완료 후 HTML/CSV만 검수하면 된다.

## V3 — D: 저용량 작업 모드

- Linux에서 D: 볼륨의 마운트 경로를 자동 탐지합니다.
- `game2.iso`를 D:의 `PSP_Localization_Work/inputs`로 SHA-1 검증 후 이동합니다.
- 프로젝트의 `game2.iso`는 외부 파일을 가리키는 심볼릭 링크로 유지합니다.
- 프리니 1/2 분석 추출본과 통합 툴 보고서를 D:에서 생성합니다.
- 각 게임 분석 직후 임시 추출본을 삭제합니다.
- `progress.txt`, `status.json`, 바탕화면 알림으로 진행 상태를 남깁니다.
- 홈 디스크에서 대용량 작업을 시작하기 전에 D: 준비 실패 시 안전하게 중단합니다.
