# 프리니 1·2 한국어 패치 v1.0 통합 공개본

2026-08-13 갱신

- 프리니 1과 프리니 2를 `prinnyk`의 한 릴리스 게시물로 통합
- PSP 표준 서명 `EBOOT.BIN` 및 PS Vita Adrenaline용 `BOOT.BIN` 이중 실행 경로 적용
- 기존 `80020148` 실기 오류 대응
- 프리니 2 스테이지 리포트의 마지막 일본어 도장 번역
- ISO9660 디렉터리 레코드와 볼륨 크기 재검증
- 두 xdelta 모두 원본에서 역적용 후 완성 ISO와 바이트 단위 일치 확인

실기 환경은 기기·CFW·ISO 드라이버에 따라 차이가 날 수 있습니다
문제가 있으면 기종, CFW/Adrenaline 버전, ISO 드라이버와 오류 코드를 함께 제보해 주십시오
