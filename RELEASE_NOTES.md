# 프리니 1·2 한국어 패치 v1.2 최종 배포본

2026-08-14 확정

## 기본 게임

- 프리니 1 내부 최종 빌드: `V7.15.62`
- 프리니 2 내부 최종 빌드: `user_screen_fixes_20260814_v59`
- 기존 최종 ISO 패치 내용을 유지하면서 공개 파일명을 모두 `v1.2`로 변경
- 게임 ISO는 제공하지 않으며 무수정 일본판 ISO에 xdelta3로 적용

## v1.2 신규 DLC 한글화

- 프리니 1 `DL0000.EDAT`: 마왕성과 타천사
- 프리니 1 `DL0001.EDAT`: 유령섬의 마로네
- 프리니 1 `DL0002.EDAT`: 연무성의 아사기 인형
- 프리니 2 `JP00.EDAT`: 초절마계 프라마임
- 프리니 2 DLC 게임 ID를 `NPJH50211`로 교정
- DLC는 ISO에 합치지 않고 `PSP/GAME/<게임 ID>`에 별도 설치

## 검증

- DLC 원본·한글판 EDAT 구조와 문자열 재파싱 검증 통과
- 기존 최종판 폰트 문자 수용 여부 검증 통과
- 개별 xdelta 6개 역적용 결과 SHA-256 일치
- DLC 배포 ZIP 4개 개별 적용 시험 통과
- 프리니 1 DLC 패치 3개 동일 출력 폴더 누적 적용 시험 통과
- 알 수 없는 기존 파일을 덮어쓰지 않는 실패 닫힘 시험 통과
- 기본 게임 ISO 패치의 기존 확정 SHA-256 유지

실기 환경은 기기·CFW·ISO 드라이버에 따라 차이가 날 수 있습니다. 문제가 있으면
기종, CFW/Adrenaline 버전, ISO 드라이버, 오류 코드와 DLC 파일명을 함께 제보해
주십시오.
